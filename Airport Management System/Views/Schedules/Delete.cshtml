@model Airport_Management_System.Models.Schedule

@{
    ViewBag.Title = "Delete";
    Layout = "~/Views/Shared/other.cshtml";
}
@section Main{
    <h2 class="form-horizontal fw-bold text-gray-800">Delete</h2>

    <h3 class="form-horizontal fw-bold text-gray-800">Are you sure you want to delete this Schedule?</h3>
    <div>
        @*<h4>Schedule</h4>*@
        <hr />
        <dl class="dl-horizontal">
            <dt class="form-horizontal fw-bold text-gray-800">
                <span>Schedule No</span>
                @*@Html.DisplayNameFor(model => model.Schedule_No)*@
            </dt>

            <dd>
                @Html.DisplayFor(model => model.Schedule_No)
            </dd>
            <dt class="form-horizontal fw-bold text-gray-800">
                <span>Airport Name</span>
                @*@Html.DisplayNameFor(model => model.Airport.Airport_Name)*@
            </dt>

            <dd>
                @Html.DisplayFor(model => model.Airport.Airport_Name)
            </dd>

        </dl>

        @using (Html.BeginForm())
        {
            @Html.AntiForgeryToken()

            <div class="form-actions no-color form-horizontal fw-bold text-gray-800">
                <input type="submit" value="Delete" class="btn btn-default" /> |
                @Html.ActionLink("Back to List", "Index")
            </div>
        }
    </div>
    }