@model Airport_Management_System.Models.Airline

@{
    ViewBag.Title = "Delete";
    Layout = "~/Views/Shared/index.cshtml";
}
@section Main{
    <h2 class="card-label fw-bold text-gray-800">Delete</h2>

    <h3 class="card-label fw-bold text-gray-800">Are you sure you want to delete this Airline?</h3>
    <div>
        @*<h4 class="card-label fw-bold text-gray-800">Airline</h4>*@
        <hr />
        <dl class="dl-horizontal ">

            <dt class="card-label fw-bold text-gray-800">
                <span>Airline Name</span>
                @*@Html.DisplayNameFor(model => model.Airline_Name)*@
            </dt>

            <dd>
                @Html.DisplayFor(model => model.Airline_Name)
            </dd>

            <dt class="card-label fw-bold text-gray-800">
                @Html.DisplayNameFor(model => model.Country)
            </dt>

            <dd>
                @Html.DisplayFor(model => model.Country)
            </dd>

            <dt class="card-label fw-bold text-gray-800">
                @Html.DisplayNameFor(model => model.Since)
            </dt>

            <dd>
                @Html.DisplayFor(model => model.Since)
            </dd>

        </dl>

        @using (Html.BeginForm())
        {
            @Html.AntiForgeryToken()

            <div class="form-actions no-color card-label fw-bold text-gray-800">
                <input type="submit" value="Delete" class="btn btn-default card-label fw-bold text-gray-800" /> |
                @Html.ActionLink("Back to List", "Index")
            </div>
        }
    </div>
    }