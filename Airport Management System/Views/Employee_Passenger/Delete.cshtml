@model Airport_Management_System.Models.Employee_Passenger

@{
    ViewBag.Title = "Delete";
    Layout = "~/Views/Shared/other.cshtml";
}
@section Main{
    <h2 class="fw-bold text-dark">Delete</h2>

    <h3 class="fw-bold text-dark">Are you sure you want to delete this Employee_Passenger Data?</h3>
    <div>
        @*<h4>Employee_Passenger</h4>*@
        <hr />
        <dl class="dl-horizontal">
            <dt class="fw-bold text-dark">
                <span>Serving Time</span>
                @*@Html.DisplayNameFor(model => model.Date)*@
            </dt>

            <dd>
                @Html.DisplayFor(model => model.Date)
            </dd>

            <dt class="fw-bold text-dark">
                <span>Employee ID</span>
                @*@Html.DisplayNameFor(model => model.Employee.Emp_ID)*@
            </dt>

            <dd>
                @Html.DisplayFor(model => model.Employee.Emp_ID)
            </dd>

            <dt class="fw-bold text-dark">
                <span>Passenger CNIC</span>
                @*@Html.DisplayNameFor(model => model.Passenger.CNIC)*@
            </dt>

            <dd>
                @Html.DisplayFor(model => model.Passenger.CNIC)
            </dd>

        </dl>

        @using (Html.BeginForm())
        {
            @Html.AntiForgeryToken()

            <div class="form-actions no-color fw-bold text-dark">
                <input type="submit" value="Delete" class="btn btn-default" /> |
                @Html.ActionLink("Back to List", "Index")
            </div>
        }
    </div>
    }